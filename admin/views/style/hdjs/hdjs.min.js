function hd_ajax(e, t, n, a) {
	var a = a || 2;
	$.ajax({
		url: e,
		data: t,
		type: "POST",
		dataType: "json",
		success: function(e) {
			hd_alert({
				message: e.message,
				timeout: a,
				success: function() {
					e.status && n && (location.href = n)
				}
			})
		}
	})
}

function hd_submit(e, t, n, a) {
	var a = a || 2;
	return $(e).data("isPost") || ($(e).data("isPost", 1), $.ajax({
		url: t,
		data: $(e).serialize(),
		type: "POST",
		dataType: "json",
		success: function(t) {
			hd_alert({
				message: t.message,
				timeout: a,
				success: function() {
					t.status && n && (location.href = n)
				}
			}), $(e).removeData("isPost")
		}
	})), !1
}

function hd_alert(e) {
	e.timeout = e.timeout || 3, hd_alert_close();
	var t = '<div id="hd-alert">' + e.message + "</div>";
	$("body").append(t), $("#hd-alert").animate({
		top: "-30px"
	}, 500).delay(1e3 * e.timeout).animate({
		top: "-100px"
	}, 500, "", function() {
		e.success && e.success()
	})
}

function hd_alert_close() {
	$("#hd-alert").remove()
}

function hd_confirm(e) {
	e.title = e.title || "温馨提示", e.message = e.message || "这是提示信息", hd_confirm_success_handlel = e.success, hd_confirm_cancel_handle = e.cancel, hd_confirm_close();
	var t = '<div id="hd-confirm-bg">背景遮罩</div>    <div id="hd-confirm">    <div class="hd-confirm-header">        <span class="hd-confirm-title">' + e.title + '</span>        <span class="hd-confirm-close" onclick="hd_confirm_close();"></span>    </div>    <div class="hd-confirm-message">' + e.message + '</div>        <div class="hd-confirm-btn">            <a class="hd-btn hd-btn-success" href="javascript:;" onclick="hd_confirm_success()">确 定</a>&nbsp;&nbsp;            <a class="hd-btn" href="javascript:;" onclick="hd_confirm_close()">关 闭</a>        </div>    </div>';
	$("body").append(t)
}

function hd_confirm_success() {
	return $("#hd-confirm").remove(), $("#hd-confirm-bg").remove(), hd_confirm_success_handlel && hd_confirm_success_handlel(), !0
}

function hd_confirm_close() {
	return $("#hd-confirm").remove(), $("#hd-confirm-bg").remove(), hd_confirm_cancel_handle && hd_confirm_cancel_handle(), !1
}

function hd_select_all(e) {
	$(e).find($("[type='checkbox']")).attr("checked", "checked")
}

function hd_reverse_select(e) {
	$(e).find($("[type='checkbox']")).attr("checked", function() {
		return 1 == !$(this).attr("checked")
	})
}

// 取得上传图片的跳出匡
function UploadThumb(mid, name) {
    var url = "/admin/index.php/qiniu?mid=" + mid + '&name=' + name;
    hd_modal({
        width: 650,//宽度
        height: 450,//高度
        title: '上传缩略图',//标题
        content: '<iframe src="' + url + '" style="height:360px;"></iframe>',//提示信息
        button: false,//显示按钮
        button_success: "确定",//确定按钮文字
        button_cancel: "关闭",//关闭按钮文字
        timeout: 0,//自动关闭时间 0：不自动关闭
        shade: true,//背景遮罩
        shadeOpacity: 0.1//背景透明度
    });
}

//弹出框隐藏掉
function removeThumb() {
    $("#thumbImg").attr('src', './img/upload_pic.png');
    // $("input[name='thumb']").val('');
}
function hd_modal(e) {
	e.width = e.width || "500", e.height = e.height || "200";
	var t = -1 * e.width / 2;
	if (e.title = e.title || "温馨提示", e.shade = void 0 != e.shade ? e.shade : !0, e.shadeOpacity = e.shadeOpacity || .9, e.timeout = e.timeout || 0, e.button = void 0 != e.button ? e.button : !0, e.button_success = e.button_success, e.button_cancel = e.button_cancel, e.content = e.content || "", hd_modal_success_handlel = e.success, hd_modal_cancel_handle = e.cancel, hd_remove_model(), e.button) {
		var n = '<div class="hd-modal-btn">';
		e.button_success && (n += '<a class="hd-btn hd-btn-success" href="javascript:;" onclick="hd_modal_success()">' + e.button_success + "</a>&nbsp;"), e.button_cancel && (n += '<a class="hd-btn" href="javascript:;" onclick="hd_modal_close()">' + e.button_cancel + "</a>"), n += "</div>"
	} else var n = "";
	shadeHtml = e.shade ? '<div id="hd-modal-bg" style="opacity:' + e.shadeOpacity + ";filter:alpha(opacity=" + 10 * e.shadeOpacity + ');">背景遮罩</div>' : "";
	var a = shadeHtml + '    <div id="hd-modal" style="width:' + e.width + "px;height:" + e.height + "px;margin-left:" + t + 'px">        <div class="hd-modal-wrap" style="height:' + e.height + 'px">            <div class="hd-modal-header">                <span class="hd-modal-title">' + e.title + '</span>                <span class="hd-modal-close" onclick="hd_modal_close();"></span>            </div>            <div class="hd-modal-message">' + e.content + "</div>            " + n + "        </div>    </div>";
	$("body").append(a), e.timeout > 0 && setTimeout(function() {
		hd_remove_model()
	}, 1e3 * e.timeout)
}

function hd_remove_model() {
	$("#hd-modal").remove(), $("#hd-modal-bg").remove()
}

function hd_modal_success() {
	return hd_remove_model(), hd_modal_success_handlel && hd_modal_success_handlel(), !0
}

function hd_modal_close() {
	return hd_remove_model(), hd_modal_cancel_handle && hd_modal_cancel_handle(), !1
}
var hd_confirm_success_handlel, hd_confirm_cancel_handle, cookie = {
		set: function(e, t, n) {
			var a = new Date;
			a.setDate(a.getDate() + n), document.cookie = e + "=" + t + ";expires=" + a
		},
		get: function(e) {
			for (var t = document.cookie.split("; "), n = t.length - 1; n >= 0; n--) {
				var a = t[n].split("=");
				if (a[0] === e) return a[1]
			}
			return ""
		},
		del: function(e) {
			cookie.set(e, 1, -1)
		}
	},
	hd_modal_success_handlel, hd_modal_cancel_handle;
$.fn.extend({
	slide: function(e) {
		function t(t) {
			s = void 0 != t ? t : s;
			var n = e.alink.eq(s);
			n.show(), e.alink.not(n).hide();
			var a = e.alink.eq(s).attr("title"),
				i = e.alink.eq(s).attr("href"),
				c = "<a style='text-decoration:none;color:" + e.textColor + ";' href='" + i + "'>" + a + "</a>";
			$("#_slideText").html("<div style='font-size:12px;line-height: 30px;text-indent: 10px;'>" + c + "</div>"), $("#_slideNum li").css({
				background: "#fff",
				color: "#333",
				"list-style": "none",
				"font-size": "12px"
			}), $("#_slideNum li").eq(s).css({
				background: "#FF7F00",
				color: "#fff"
			}), s = s + 1 >= e.num ? 0 : s + 1
		}
		_bgcolor = e.bgcolor ? e.bgcolor : "#333", e.bgopacity = e.bgopacity || .9, e.textColor = e.textColor || "#ddd", e.timeout = e.timeout || 3, e.alink = $(this).find("a"), e.num = e.alink.length, $(this).css({
			width: e.width,
			height: e.height,
			overflow: "hidden",
			position: "relative"
		}), e.alink.hide(), e.alink.eq(0).show();
		var n = "<div id='_slideText' style='height: 30px;background:" + _bgcolor + ";color:#fff;position: absolute;left: 0px;right: 0px;bottom: 0px;'></div>";
		$(this).append(n), $("#_slideText").css({
			opacity: e.bgopacity
		});
		for (var a = "<div id='_slideNum' style='position: absolute;height: 18px;bottom: 6px;right: 0px;'>", i = 1; e.num >= i; i++) a += "<li style='float: left;opacity:0.8;border-radius:50%;cursor:pointer;color:#333;width: 18px;height: 18px;background: #fff;margin-right: 6px;text-align: center;line-height: 1.5em;'>" + i + "</li>";
		a += "</div>", $(this).append(a);
		var s = 0;
		setInterval(t, 1e3 * e.timeout), t(0), $("#_slideNum li").mouseover(function() {
			var e = $(this).index();
			t(e)
		})
	}
}), 
$(function() {
	$("div.hd-tab ul.hd-tab-menu li").click(function() {
		$("div.hd-tab-content").children("div").hide();
		var e = $(this).attr("lab");
		$("div.hd-tab-content div[lab='" + e + "']").show(), $("div.hd-tab ul.hd-tab-menu li").removeClass("active"), $("div.hd-tab ul.hd-tab-menu li[lab='" + e + "']").addClass("active")
	})
})